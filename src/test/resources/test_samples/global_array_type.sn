let g_simple_array: [i32; 5];
let g_initialized_array: [bool; 3] = [true, false, true];
// let g_multidim_array: [[i32; 2]; 3]; // Если поддерживается, раскомментировать и проверить

fn main() -> void {
    // Тест на объявление глобальных переменных типа массив
    // Функциональность будет проверяться в других тестах
}
